server:
  port: 99
  servlet:
    session:
      timeout: 10m

spring:
  application:
    name: goop-auth
  #cloud配置
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.208.38:8848

  #Redis配置
  redis:
    host: 192.168.109.134
  #SpringSession配置
  session:
    store-type: redis
